import 'package:flutter/material.dart';

class Apartments extends StatelessWidget {
  static String id = 'Apartment';
  const Apartments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
    );
  }
}
